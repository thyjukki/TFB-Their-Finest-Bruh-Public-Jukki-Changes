operations = {

	ROM_expand_greater_romania = {

		icon = generic_nationalism

		allowed = {
			original_tag = ROM
		}

		fire_only_once = yes
		
		available = {

			ROM = { has_completed_focus = ROM_the_transnistria_compromise }
			has_full_control_of_state = 192
			has_full_control_of_state = 198
			has_full_control_of_state = 834
		}

		visible = {

		}
		
		complete_effect = {
			add_state_core = 192
			add_state_core = 198
			add_state_core = 834

			192 = {			
				add_dynamic_modifier = { modifier = ROM_unintegrated_territory }
			}
			198 = {			
				add_dynamic_modifier = { modifier = ROM_unintegrated_territory }
				}
			834 = {			
				add_dynamic_modifier = { modifier = ROM_unintegrated_territory }
				}
		}
	}

	ROM_reclaim_territory = {

		icon = generic_nationalism

		allowed = {
			original_tag = ROM
		}

		fire_only_once = yes
		
		available = {
			SOV = {
				owns_state = 80
				owns_state = 78
				owns_state = 766
			}
			has_full_control_of_state = 80
			has_full_control_of_state = 78
			has_full_control_of_state = 766
		}

		visible = {
			SOV = {
				owns_state = 80
				owns_state = 78
				owns_state = 766
			}
		}
		
		complete_effect = {
			add_political_power = 100
			add_stability = 0.1
			transfer_state = 80
			transfer_state = 78
			transfer_state = 766
		}
	}
	
	ROM_michaels_coup = {

		icon = generic_tank

		available = {
			GER = { divisions_in_state = { state = 46 size > 3 } }
		}

		days_mission_timeout = 7
		fire_only_once = yes

		activation = {
			always = no
		}

		complete_effect = {
			every_state = {
				limit = {
					is_fully_controlled_by = ROM
					NOT = { is_owned_by = ROM }
				}
				transfer_state_to = GER
			}
			set_autonomy = {
				target = ROM
				autonomy_state = autonomy_reichsprotectorate
			}
		}

		is_good = no

		timeout_effect = {
			SOV = {
				custom_effect_tooltip = needs_approval
				country_event = DOD_romania.501
			}
			effect_tooltip = {
				ROM = {
					leave_faction = yes
					white_peace = SOV
					every_country = {
						limit = {
							has_war_with = ROM
							is_in_faction_with = ENG
						}
						white_peace = ROM
					}
				}
				SOV = { 
					add_to_faction = ROM
					transfer_state = 78
					transfer_state = 766
					transfer_state = 80
				}
				create_wargoal = {
					type = puppet_wargoal_focus
					target = ROM
				}
			}
		}
	}
	
	ROM_surrender_soviets = {
		icon = generic_nationalism

		allowed ={
			original_tag = ROM
		}

		fire_only_once = yes
		
		visible = {
			has_war_with = SOV
			NOT = { has_country_flag = bessarabia_finished  }
		}

		state_target = yes
		on_map_mode = map_and_decisions_view
		targets = {
			46
    	}

		days_remove = 7

		available = {
			has_war_with = SOV
			has_civil_war = no
			is_in_faction = no
			OR = {
				casualties > 50000
				surrender_progress > 0.20
				NOT = {
					78 = {
						CONTROLLER = {
							OR = {
								tag = ROM
								is_subject_of = ROM
							}
						}
					}
				}
			}
		}

		remove_effect = {
			SOV = {
				white_peace = ROM
 				transfer_state = 78
				transfer_state = 80
 				transfer_state = 766
			}
			ROM = { set_country_flag = bessarabia_finished }
			diplomatic_relation = {
				country = SOV
				relation = non_aggression_pact
			}
		}
		ai_will_do = {
			factor = 20
		}
	}
	
	ROM_victory_soviets = {
		icon = generic_nationalism
		allowed = {
			original_tag = ROM
		}
		fire_only_once = yes
		visible = {
			has_war_with = SOV
			NOT = { has_country_flag = bessarabia_finished  }
		}
		fire_only_once = yes

		state_target = yes
		on_map_mode = map_and_decisions_view
		targets = {
			202
    	}

		available = {
			has_war_with = SOV
			NOT = { has_war_with = GER }
			OR = {
				SOV = {
					casualties > 250000
				}
				202 = {
					CONTROLLER = {
						OR = {
							tag = ROM
							is_subject_of = ROM
						}
					}
				}
			}
		}
		days_remove = 7
		remove_effect = {
			SOV = {
				white_peace = ROM
			}
			ROM = { 
				set_country_flag = bessarabia_finished
				set_country_flag = ROM_won_bessarabia
			}
			diplomatic_relation = {
				country = SOV
				relation = non_aggression_pact
			}
		}
		ai_will_do = {
			factor = 200
		}
	}

	ROM_iron_guard_coup = {
		icon = generic_nationalism
		allowed = {
			original_tag = ROM
		}
		visible = {
			has_completed_focus = ROM_force_abdication
		}

		available = {
			hidden_trigger = {
				always = no
			}
		}

		cancel_trigger = {
			OR = {
				has_completed_focus = ROM_execute_the_coup
				has_completed_focus = ROM_crush_legionnaires
			}
		}
		fire_only_once = yes

		activation = {
			has_completed_focus = ROM_force_abdication
		}
		
		days_mission_timeout = 140

		timeout_effect = {
			start_civil_war = {
				ideology = fascism
			}
		}
	}
}

