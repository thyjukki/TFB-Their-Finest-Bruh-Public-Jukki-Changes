SPR_calculate_appeasement_ticking = {
    set_variable = { spanish_tick = 0 }
    if = {
        limit = {  # D-Day
            GER = { 
                OR = {
                    has_completed_focus = GER_prepare_barb 
                    has_war_with = SOV
                }
            }
            collection_size = {
                input = {
                    input = game:all_states
                    operators = {
                        limit = {
                            is_on_continent = europe
                            has_dynamic_modifier = PACIFIC_COMMONWEALTH_allied_controlled
                        }
                    }
                }
                value > 0
            }
        }
        # Tick stays at 0
        set_variable = { spanish_tick = 0 }
    }
    else_if = { # Carlist
        limit = { 
            has_completed_focus = SPA_supremacy_of_the_communion
        }
         set_variable = { spanish_tick = 0.0013 }
    }
    else = {  # Franco
        set_variable = { spanish_tick = 0.0008 }

        # Cairo (0.01)
        if = {
            limit = {
                907 = {
                    OR = {
                        THIS.controller = { is_in_faction_with = GER }
                        THIS.controller = { is_in_faction_with = JAP }
                        THIS.controller = { is_in_faction_with = ITA }
                        THIS = {
                            OR = {
                                is_controlled_by = GER
                                is_controlled_by = JAP
                                is_controlled_by = ITA
                                is_controlled_by = FRA.SOUTHFRANCEOWNER
                            }
                        }
                    }
                }
            }
            add_to_variable = { spanish_tick = 0.0001 }
        }
        # Port Sudan (0.01)
        if = {
            limit = {
                883 = {
                    OR = {
                        THIS.controller = { is_in_faction_with = GER }
                        THIS.controller = { is_in_faction_with = JAP }
                        THIS.controller = { is_in_faction_with = ITA }
                        THIS = {
                            OR = {
                                is_controlled_by = GER
                                is_controlled_by = JAP
                                is_controlled_by = ITA
                                is_controlled_by = FRA.SOUTHFRANCEOWNER
                            }
                        }
                    }
                }
            }
            add_to_variable = { spanish_tick = 0.0001 }
        }
        # Algiers (0.01)
        if = {
            limit = {
                459 = {
                    OR = {
                        THIS.controller = { is_in_faction_with = GER }
                        THIS.controller = { is_in_faction_with = JAP }
                        THIS.controller = { is_in_faction_with = ITA }
                        THIS = {
                            OR = {
                                is_controlled_by = GER
                                is_controlled_by = JAP
                                is_controlled_by = ITA
                                AND = {
                                    is_controlled_by = FRA.SOUTHFRANCEOWNER
                                    any_country_with_original_tag = {
                                        original_tag_to_check = FRA
                                        has_government = fascism
                                        OR = {
                                            is_subject_of = GER
                                            has_focus_tree = vichy_french_focus
                                        }
                                        NOT = { has_country_flag = case_anton }
                                        has_war_with = ENG
                                    }
                                }
                                
                            }
                        }
                    }
                }
            }
            add_to_variable = { spanish_tick = 0.0001 }
        }
        # Aden (0.02)
        if = {
            limit = {
                992 = {
                    OR = {
                        THIS.controller = { is_in_faction_with = GER }
                        THIS.controller = { is_in_faction_with = JAP }
                        THIS.controller = { is_in_faction_with = ITA }
                        THIS = {
                            OR = {
                                is_controlled_by = GER
                                is_controlled_by = JAP
                                is_controlled_by = ITA
                                is_controlled_by = FRA.SOUTHFRANCEOWNER
                            }
                        }
                    }
                }
            }
            add_to_variable = { spanish_tick = 0.0002 }
        }
        # Gibraltar (0.04)
        if = {
            limit = {
                118 = {
                    OR = {
                        THIS.controller = { is_in_faction_with = GER }
                        THIS.controller = { is_in_faction_with = JAP }
                        THIS.controller = { is_in_faction_with = ITA }
                        THIS = {
                            OR = {
                                is_controlled_by = GER
                                is_controlled_by = JAP
                                is_controlled_by = ITA
                                is_controlled_by = FRA.SOUTHFRANCEOWNER
                            }
                        }
                    }
                }
            }
            add_to_variable = { spanish_tick = 0.0004 }
        }
        # Cape Town (0.04)
        if = {
            limit = {
                681 = {
                    OR = {
                        THIS.controller = { is_in_faction_with = GER }
                        THIS.controller = { is_in_faction_with = JAP }
                        THIS.controller = { is_in_faction_with = ITA }
                        THIS = {
                            OR = {
                                is_controlled_by = GER
                                is_controlled_by = JAP
                                is_controlled_by = ITA
                                is_controlled_by = FRA.SOUTHFRANCEOWNER
                            }
                        }
                    }
                }
            }
            add_to_variable = { spanish_tick = 0.0004 }
        }
    }

    SPR = { set_variable = { SPR_power_balance_daily = spanish_tick } }
}